---
phase: 01-secure-raw-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/components/layouts/DashboardLayout.tsx
  - frontend/src/app/dashboard/reports/page.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/reports.ts
  - frontend/src/components/reports/RawAttendanceTable.tsx
  - frontend/src/components/reports/ReportFilters.tsx
autonomous: false
requirements:
  - SEC-01
  - RPT-02
  - QRY-01
  - QRY-02
  - QRY-05
user_setup: []

must_haves:
  truths:
    - Admin/Instructor ser Reports i nav och kan öppna sidan
    - Reports visar råtabell (1 rad per attendance) med paginering
    - Filter (datumintervall, student, schema, instruktör, status) påverkar resultat
  artifacts:
    - frontend/src/app/dashboard/reports/page.tsx exists
    - frontend/src/components/layouts/DashboardLayout.tsx contains Reports link with role gating
  key_links:
    - UI calls POST /api/reports/attendance/raw via fetchWithAuth
---

<objective>
Implementera Reports-sidan (Phase 1 UI) för rådata: filter + tabell + paginering, och rollstyrd navigation.

Purpose: Gör Phase 1 användbar för admin/instruktörer i UI.
Output: `/dashboard/reports` med filterkontroller och tabell kopplad till backend-API.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@~/.copilot/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-secure-raw-reporting/01-CONTEXT.md
@frontend/src/components/layouts/DashboardLayout.tsx
@frontend/src/lib/api.ts
@frontend/src/components/ui/table.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/select.tsx
@frontend/src/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lägg till Reports i dashboard-nav med rollgating</name>
  <files>frontend/src/components/layouts/DashboardLayout.tsx</files>
  <read_first>frontend/src/components/layouts/DashboardLayout.tsx</read_first>
  <action>
Lägg till navLink:
- href: `/dashboard/reports`
- label: `Reports`
- roles: `["admin", "instructor"]`

Säkerställ att den inte syns för staff/student.
  </action>
  <verify>cd frontend && pnpm run lint || true</verify>
  <acceptance_criteria>
    - Nav innehåller Reports för admin/instructor
  </acceptance_criteria>
  <done>Reports-länk syns för rätt roller</done>
</task>

<task type="auto">
  <name>Task 2: Skapa Reports page med default datumintervall (senaste 30 dagar)</name>
  <files>frontend/src/app/dashboard/reports/page.tsx</files>
  <read_first>.planning/phases/01-secure-raw-reporting/01-CONTEXT.md</read_first>
  <action>
Skapa sidan `/dashboard/reports` (client component vid behov).

Beteende:

- Sätt default `from/to` = senaste 30 dagar (date-only `YYYY-MM-DD`).
- Visa filterkontroller och tabell.

OBS: Phase 1 innehåller INTE kolumn-toggle, sortering eller global sök.
  </action>
  <verify>cd frontend && pnpm run dev</verify>
  <acceptance_criteria>
    - Sidan renderar utan runtime errors
    - Default från/till är satta
  </acceptance_criteria>
  <done>Reports page finns och har initial state</done>
</task>

<task type="auto">
  <name>Task 3: Implementera filter-UI med befintliga UI-primitives</name>
  <files>frontend/src/components/reports/ReportFilters.tsx</files>
  <read_first>frontend/src/components/ui/input.tsx, frontend/src/components/ui/select.tsx, frontend/src/components/ui/checkbox.tsx</read_first>
  <action>
Bygg filter-UI utan att introducera nya shadcn primitives (håll dig till befintliga):
- Datum: två `<Input type="date">` för from/to
- Status multi-select: 4 checkboxes (present/absent/late/excused)
- Student:
  - Dropdown (Select) för studentId (populera med `GET /api/students`, map `_id` + `name`)
  - Text input för studentName (contains)
- Schema (classScheduleId): Select som laddas via `GET /api/schedules?startDate=...&endDate=...` och begränsas till datumintervall
- Instruktör: Select (värden hämtas/deriveras från classes eller från schedule-populate)

Koppla filter state till page och trigga refresh på “Apply” (eller auto-refresh med debouncing om det är enkelt).
  </action>
  <verify>cd frontend && pnpm run lint || true</verify>
  <acceptance_criteria>
    - Alla filter i CONTEXT finns i UI
    - Status kan markeras i flera samtidigt
  </acceptance_criteria>
  <done>Filterkomponenten producerar query-object för API</done>
</task>

<task type="auto">
  <name>Task 4: Implementera råtabell + paginering kopplad till report-API</name>
  <files>frontend/src/components/reports/RawAttendanceTable.tsx, frontend/src/types/reports.ts</files>
  <read_first>frontend/src/components/ui/table.tsx, .planning/phases/01-secure-raw-reporting/01-CONTEXT.md</read_first>
  <action>
- Skapa typ(er) i `frontend/src/types/reports.ts` för API-responsen.
- Rendera tabell med kolumnerna från CONTEXT.
- Implementera paginering (page/pageSize=25). Visa total och "Prev/Next".
- API-call: `api.post('/api/reports/attendance/raw', body)`.

Se till att UI hanterar tomma resultat och fel (t.ex. visa enkel feltext).
  </action>
  <verify>cd frontend && pnpm run dev</verify>
  <acceptance_criteria>
    - Tabellen visar rader med student/klass/datum/status
    - Paginering ändrar resultat och respekterar total
  </acceptance_criteria>
  <done>UI kan hämta och visa paginerade raw rows</done>
</task>

<task type="auto">
  <name>Task 5: Starta dev-miljö för visuell verifiering</name>
  <files>scripts/start-dev.sh</files>
  <read_first>README.md</read_first>
  <action>
Starta backend + frontend dev (t.ex. via `./scripts/start-dev.sh`) och verifiera att ports är up.
  </action>
  <verify>curl -sSf http://localhost:4000/api/health && curl -sSf http://localhost:4001/login</verify>
  <done>Dev-servrar kör och är nåbara</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reports sida (/dashboard/reports) med filter + råtabell + paginering</what-built>
  <how-to-verify>
    1. Logga in som admin eller instructor
    2. Gå till /dashboard/reports
    3. Verifiera att default från/till = senaste 30 dagar
    4. Verifiera att status-checkboxar filtrerar korrekt (välj t.ex. bara present)
    5. Verifiera att student dropdown + student text kan filtrera
    6. Verifiera att schema-dropdown bara listar pass inom datumintervall
    7. Verifiera att paginering (Prev/Next) fungerar och total visas
  </how-to-verify>
  <resume-signal>Svara "approved" eller beskriv vad som är fel</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend endpoint fungerar (från Plan 01)
- [ ] Frontend bygger/rendrar utan runtime errors
- [ ] Manuell UI-verifiering enligt checkpoint
</verification>

<success_criteria>

- ADMIN/INSTRUCTOR kan använda Reports för rådata med filter och paginering
- Funktionalitet matchar Phase 1 CONTEXT och roadmap success criteria

</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-raw-reporting/01-02-SUMMARY.md`
</output>
