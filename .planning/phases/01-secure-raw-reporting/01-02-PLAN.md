---
phase: 01-secure-raw-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/components/layouts/DashboardLayout.tsx
  - frontend/src/app/dashboard/reports/page.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/reports.ts
  - frontend/src/components/reports/RawAttendanceTable.tsx
  - frontend/src/components/reports/ReportFilters.tsx
autonomous: false
requirements:
  - SEC-01
  - RPT-02
  - QRY-01
  - QRY-02
  - QRY-05
user_setup: []

must_haves:
  truths:
    - Admin/Instructor can see Reports in the nav and open the page
    - Reports displays a raw table (1 row per attendance) with pagination
    - Filters (date range, student, schedule, instructor, status) affect the results
  artifacts:
    - frontend/src/app/dashboard/reports/page.tsx exists
    - frontend/src/components/layouts/DashboardLayout.tsx contains Reports link with role gating
  key_links:
    - UI calls POST /api/reports/attendance/raw via fetchWithAuth
---

<objective>
Implement the Reports page (Phase 1 UI) for raw data: filters + table + pagination, and role-controlled navigation.

Purpose: Make Phase 1 usable for admins/instructors in the UI.
Output: `/dashboard/reports` with filter controls and a table wired to the backend API.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
@~/.copilot/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-secure-raw-reporting/01-CONTEXT.md
@frontend/src/components/layouts/DashboardLayout.tsx
@frontend/src/lib/api.ts
@frontend/src/components/ui/table.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/select.tsx
@frontend/src/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Reports to the dashboard nav with role gating</name>
  <files>frontend/src/components/layouts/DashboardLayout.tsx</files>
  <read_first>frontend/src/components/layouts/DashboardLayout.tsx</read_first>
  <action>
Add a navLink:
- href: `/dashboard/reports`
- label: `Reports`
- roles: `["admin", "instructor"]`

Make sure it is not visible to staff/student.
  </action>
  <verify>cd frontend && pnpm run lint || true</verify>
  <acceptance_criteria>
    - Nav contains Reports for admin/instructor
  </acceptance_criteria>
  <done>The Reports link is visible for the correct roles</done>
</task>

<task type="auto">
  <name>Task 2: Create the Reports page with a default date range (last 30 days)</name>
  <files>frontend/src/app/dashboard/reports/page.tsx</files>
  <read_first>.planning/phases/01-secure-raw-reporting/01-CONTEXT.md</read_first>
  <action>
Create the `/dashboard/reports` page (client component if needed).

Behavior:

- Set default `from/to` = last 30 days (date-only `YYYY-MM-DD`).
- Show filter controls and the table.

NOTE: Phase 1 does NOT include column toggles, sorting, or global search.
  </action>
  <verify>cd frontend && pnpm run dev</verify>
  <acceptance_criteria>
    - The page renders without runtime errors
    - Default from/to are set
  </acceptance_criteria>
  <done>The Reports page exists and has initial state</done>
</task>

<task type="auto">
  <name>Task 3: Implement the filter UI using existing UI primitives</name>
  <files>frontend/src/components/reports/ReportFilters.tsx</files>
  <read_first>frontend/src/components/ui/input.tsx, frontend/src/components/ui/select.tsx, frontend/src/components/ui/checkbox.tsx</read_first>
  <action>
Build the filter UI without introducing new shadcn primitives (stick to the existing ones):
- Date: two `<Input type="date">` for from/to
- Status multi-select: 4 checkboxes (present/absent/late/excused)
- Student:
  - Dropdown (Select) for studentId (populate with `GET /api/students`, map `_id` + `name`)
  - Text input for studentName (contains)
- Schedule (classScheduleId): Select loaded via `GET /api/schedules?startDate=...&endDate=...` and limited to the date range
- Instructor: Select (values fetched/derived from classes or from schedule-populate)

Connect filter state to the page and trigger refresh on “Apply” (or auto-refresh with debouncing if that is simple).
  </action>
  <verify>cd frontend && pnpm run lint || true</verify>
  <acceptance_criteria>
    - All filters in CONTEXT exist in the UI
    - Multiple status values can be selected at the same time
  </acceptance_criteria>
  <done>The filter component produces a query object for the API</done>
</task>

<task type="auto">
  <name>Task 4: Implement the raw table + pagination wired to the report API</name>
  <files>frontend/src/components/reports/RawAttendanceTable.tsx, frontend/src/types/reports.ts</files>
  <read_first>frontend/src/components/ui/table.tsx, .planning/phases/01-secure-raw-reporting/01-CONTEXT.md</read_first>
  <action>
- Create type(s) in `frontend/src/types/reports.ts` for the API response.
- Render a table with the columns from CONTEXT.
- Implement pagination (`page`/`pageSize=25`). Show total and "Prev/Next".
- API call: `api.post('/api/reports/attendance/raw', body)`.

Make sure the UI handles empty results and errors (e.g. show a simple error message).
  </action>
  <verify>cd frontend && pnpm run dev</verify>
  <acceptance_criteria>
    - The table shows rows with student/class/date/status
    - Pagination changes the results and respects total
  </acceptance_criteria>
  <done>The UI can fetch and display paginated raw rows</done>
</task>

<task type="auto">
  <name>Task 5: Start the dev environment for visual verification</name>
  <files>scripts/start-dev.sh</files>
  <read_first>README.md</read_first>
  <action>
Start backend + frontend dev (e.g. via `./scripts/start-dev.sh`) and verify that the ports are up.
  </action>
  <verify>curl -sSf http://localhost:4000/api/health && curl -sSf http://localhost:4001/login</verify>
  <done>Dev servers are running and reachable</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reports page (/dashboard/reports) with filters + raw table + pagination</what-built>
  <how-to-verify>
    1. Log in as admin or instructor
    2. Go to /dashboard/reports
    3. Verify that the default from/to = the last 30 days
    4. Verify that the status checkboxes filter correctly (e.g. select only present)
    5. Verify that the student dropdown + student text can filter
    6. Verify that the schedule dropdown only lists sessions within the date range
    7. Verify that pagination (Prev/Next) works and total is shown
  </how-to-verify>
  <resume-signal>Reply "approved" or describe what is wrong</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend endpoint works (from Plan 01)
- [ ] Frontend builds/renders without runtime errors
- [ ] Manual UI verification according to the checkpoint
</verification>

<success_criteria>

- ADMIN/INSTRUCTOR can use Reports for raw data with filters and pagination
- Functionality matches the Phase 1 CONTEXT and roadmap success criteria

</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-raw-reporting/01-02-SUMMARY.md`
</output>
