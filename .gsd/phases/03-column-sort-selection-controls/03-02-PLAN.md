---
phase: 03-column-sort-selection-controls
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/app/dashboard/reports/page.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - Användaren kan skriva i en global fri-text-sök och resultat uppdateras
    - Sortering via rubriker fungerar utan att hoppa till sida 1
    - Kolumn-visa/dölj fungerar separat för Raw och Aggregated och reset:ar till defaults vid reload
    - Raw default synliga kolumner är Date/Start/End/Student/Class/Instructor/Status
  artifacts:
    - path: frontend/src/app/dashboard/reports/page.tsx
      provides: Reports UI med global sök + kolumnhantering + sort
  key_links:
    - from: frontend/src/app/dashboard/reports/page.tsx
      to: /api/reports/attendance/raw
      via: POST body
      pattern: "search"
    - from: frontend/src/app/dashboard/reports/page.tsx
      to: /api/reports/attendance/aggregate
      via: POST body
      pattern: "search"
---

# 03-02 Plan — Frontend: global search + column visibility per mode + sort UX rules

<objective>
Implementera Phase 3 UI-delen för global fri-text-sök, samt justera kolumn-visa/dölj och sort-UX enligt Phase 3 CONTEXT.

Purpose: Låsa in en stabil, förutsägbar Reports-UX där sort/sök/kolumner fungerar utan att paginering blir konstig.
Output: Uppdaterad Reports-sida med global sök och korrekt kolumn-/sortbeteende.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/phases/03-column-sort-selection-controls/03-CONTEXT.md
@frontend/src/app/dashboard/reports/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lägg till global fri-text-sök i Reports UI och skicka `search` till API</name>
  <files>frontend/src/app/dashboard/reports/page.tsx</files>
  <action>
- Lägg till state: `search` (string) med `<Input>` i filterpanelen.
- Skicka `search` i request-body när den är non-empty (både raw och aggregate).
- Page-reset:
  - När `search` ändras: reset till page 1.
  - När sort ändras: **ingen** auto-reset till page 1 (D3-06).
  </action>
  <verify>pnpm --prefix frontend build</verify>
  <done>UI visar sökfält och API-request inkluderar `search`</done>
</task>

<task type="auto">
  <name>Task 2: Kolumn-visa/dölj separat för Raw och Aggregated (utan persistens)</name>
  <files>frontend/src/app/dashboard/reports/page.tsx</files>
  <action>
- Behåll Raw-defaults exakt enligt D3-07.
- Inför en separat `aggregatedColumnVisibility` (alla relevanta kolumner synliga som default).
- När användaren växlar mode mellan raw/aggregate ska checkboxarna spegla den modens visibility-map.
- Ingen localStorage/persistens (D3-10).

Not: Behåll befintlig layout; ändra endast state/wiring.
  </action>
  <verify>pnpm --prefix frontend build</verify>
  <done>
    - Kolumnval i Raw påverkar inte Aggregated och tvärtom
    - Reload reset:ar tillbaka till respektive default
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reports: global sök + separata kolumner per läge + sort utan page-reset</what-built>
  <how-to-verify>
    1. Logga in som admin eller instructor
    2. Gå till /dashboard/reports
    3. Raw:
       - Bekräfta default kolumner: Date/Start/End/Student/Class/Instructor/Status
       - Skriv något i sök (t.ex. del av studentnamn eller klassnamn) → tabellen filtreras
    4. Sort:
       - Gå till sida 2 (Next)
       - Klicka på en kolumnrubrik för sort → sidan ska stanna kvar på sida 2 (inte hoppa till 1)
    5. Aggregated:
       - Växla till Aggregated och testa sök igen
       - Ändra kolumn-visibility (om UI finns) och växla tillbaka till Raw → Raw-val ska vara oförändrat
    6. Reload sidan → kolumner återställs till defaults
  </how-to-verify>
  <resume-signal>Svara "approved" eller beskriv vad som är fel</resume-signal>
</task>

</tasks>

<verification>
- [ ] pnpm --prefix frontend build
- [ ] Manuell verifiering enligt checkpoint
</verification>

<success_criteria>
- UI uppfyller QRY-03/QRY-04 och D3-besluten för sort/kolumner
</success_criteria>

<output>
After completion, create `.gsd/phases/03-column-sort-selection-controls/03-02-SUMMARY.md`
</output>
